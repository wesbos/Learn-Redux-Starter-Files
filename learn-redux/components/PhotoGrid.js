import React from 'react';
import { Link } from 'react-router';

const PhotoGrid = React.createClass({
  render() {
    return (
      <div className="phot-grid">
        Im the photo-grid
      </div>
    )
  }
});

export default PhotoGrid;
